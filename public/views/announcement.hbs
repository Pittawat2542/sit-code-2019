<!DOCTYPE html>
<html lang="en">

<head>
    {{> meta_data }}
    {{> meta_icon }}

    <!-- Site Properties -->
    <title>
        < SITCODE 2019 />
    </title>
    {{> link_css_index }}
</head>

<body>

    <div class="pusher">
        <div class="ui inverted vertical center aligned segment" id="logo">

            <div class="ui text container">
                <a href="/">
                    <h1 class="ui header special">
                        < SITCODE 2019 />
                    </h1>
                </a>
            </div>

        </div>

        <div class="ui vertical stripe segment">
            <h2 class="ui header center aligned">รายชื่อทีมที่มีสิทธิ์เข้าร่วมการแข่งขันรอบ Online Round</h2>
            <h4 class="ui header center aligned text-red">* ทีมที่มีสิทธิ์เข้าร่วมการแข่งขันกรุณาตรวจสอบอีเมล์ของหัวหน้าทีมเพื่อรับรหัสเข้าใช้ระบบการแข่งขัน *</h2>
            <h4 class="ui header center aligned text-red">* แข่งขัน Online ผู้เข้าแข่งขัน<span class="underline">ไม่จำเป็น</span>ต้องมาแข่งขันที่คณะเทคโนโลยีสารสนเทศ *</h2>
            <div class="ui stackable middle equal width grid container">
                <div class="centered row">
                    <div class="ten wide column">
                        <table class="ui celled blue striped selectable table">
                            <thead>
                                <tr>
                                    <th class="center aligned">ลำดับ</th>
                                    <th class="center aligned">ชื่อทีม</th>
                                    <th class="mobile-center">โรงเรียน</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each arr}}
                                <tr>
                                    <td data-label="ลำดับ" class="center aligned">{{inc @index}}</td>
                                    <td data-label="ชื่อทีม" class="center aligned">{{this.team_name}}</td>
                                    <td data-label="โรงเรียน" class="mobile-center">{{this.team_school}}</td>
                                </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        {{> footer}}
    </div>
</body>

</html>